{
  "name": "@acme/core",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc -w",
    "lint": "eslint \"src/**/*.ts*\" --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@acme/util": "*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@acme/eslint-config": "*",
    "@acme/typescript-config": "*",
    "eslint": "^8.56.0",
    "ts-essentials": "^9.4.2",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
